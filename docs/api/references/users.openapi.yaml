openapi: 3.1.0
info:
  title: z0-auth Platform Users API (removed)
  version: 0.0.0
  description: |
    **Removed in M05.** Global `/api/v1/users` conflated console operators with app end users.
    Use `app-users.openapi.yaml` for per-application end users and `invites.openapi.yaml` for console members.
paths: {}
