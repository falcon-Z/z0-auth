import { Card, CardTitle } from "@z0/components/ui/card";

export default function initialAppSetup() {
  return (
    <div class={"grid place-items-center h-full w-full"}>
      <Card></Card>
    </div>
  );
}
