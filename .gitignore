# dependencies (bun install)
node_modules

# output
out
dist
tests/e2e/.auth/
playwright-report/
test-results/
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# Instance keys (symmetric data key + Ed25519 token signing keypair)
.data/

# dotenv environment variable files (keep .env.example as the single template)
.env*
!.env.example

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

/generated/prisma

# package-lock.json is not used - this project uses bun.lock
package-lock.json
.vscode/settings.json
.kiro/*

# JWT Keypairs - Keep these secure and out of version control
keys/
*.key
*.pub
jwt-private.key
jwt-public.key

# Config files (if accidentally created)
config.json

# Local workspace notes (not committed)
.local/

# Dev database seed scripts (local QA only — not shipped in repo)
src/scripts/db-seed-dev.ts
src/scripts/dev-seed/
