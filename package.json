{
  "name": "z0-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun --filter @z0/server dev",
    "start": "bun --filter @z0/server start",
    "test": "bun test --concurrency 1",
    "db:reset": "bun --filter @z0/server db:reset",
    "db:setup": "bun --filter @z0/server db:setup"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
